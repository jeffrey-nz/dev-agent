{
  "name": "dev-agent",
  "displayName": "Dev Agent",
  "description": "Embedded AI code editor powered by browser-ai-bridge — runs the full agent pipeline inside VS Code.",
  "version": "0.1.0",
  "type": "commonjs",
  "engines": {
    "vscode": "^1.85.0",
    "node": ">=20.0.0"
  },
  "categories": ["AI", "Other"],
  "activationEvents": ["onCommand:devAgent.start", "onStartupFinished"],
  "main": "./dist/extension.cjs",
  "bin": {
    "dev-agent": "./bin/dev-agent.mjs"
  },
  "contributes": {
    "commands": [
      {
        "command": "devAgent.start",
        "title": "Dev Agent: Open"
      },
      {
        "command": "devAgent.ask",
        "title": "Dev Agent: Ask"
      },
      {
        "command": "devAgent.stop",
        "title": "Dev Agent: Stop"
      },
      {
        "command": "devAgent.selectProvider",
        "title": "Dev Agent: Select Provider"
      },
      {
        "command": "devAgent.debugSnapshot",
        "title": "Dev Agent: Debug Snapshot (save DOM to /tmp)"
      }
    ],
    "keybindings": [
      {
        "command": "devAgent.start",
        "key": "ctrl+shift+d",
        "mac": "cmd+shift+d"
      },
      {
        "command": "devAgent.stop",
        "key": "ctrl+shift+.",
        "mac": "cmd+shift+."
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "devAgent",
          "title": "Dev Agent",
          "icon": "resources/icon.svg"
        }
      ]
    },
    "views": {
      "devAgent": [
        {
          "type": "webview",
          "id": "devAgent.mainView",
          "name": "Dev Agent"
        }
      ]
    }
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch",
    "package": "vsce package",
    "lint": "eslint src/",
    "start": "node bin/dev-agent.mjs",
    "sync-modules": "rsync -a --delete ../agent-core/src/ node_modules/agent-core/src/ && rsync -a ../agent-core/package.json node_modules/agent-core/package.json && rsync -a --delete ../browser-ai-bridge/src/ node_modules/browser-ai-bridge/src/ && rsync -a --delete ../browser-ai-bridge/bin/ node_modules/browser-ai-bridge/bin/ && rsync -a ../browser-ai-bridge/package.json node_modules/browser-ai-bridge/package.json && npm install --prefix node_modules/browser-ai-bridge --no-save 2>/dev/null || true"
  },
  "dependencies": {
    "agent-core": "github:jeffrey-nz/agent-core",
    "browser-ai-bridge": "github:jeffrey-nz/browser-ai-bridge"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "esbuild": "^0.25.0",
    "@vscode/vsce": "^3.0.0"
  }
}
